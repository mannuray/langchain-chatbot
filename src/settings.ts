
/**
 * Application Settings
 * 
 * This file contains application settings that can be manually edited.
 * Edit this file to change the backend URL or other configuration options.
 */

const settings = {
  /**
   * API URL for the backend server
   * This is used for all API requests including the LangChain model API
   * and the expert advice API.
   */
  apiUrl: "http://localhost:5002",
};

export default settings;
